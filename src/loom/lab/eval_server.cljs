(ns loom.lab.eval-server
  "Lightweight form evaluation server for Lab containers.
   Accepts EDN forms over TCP, evaluates via cljs.js/eval-str,
   returns EDN results.")

(defn main []
  (println "Loom eval server starting...")
  ;; TODO: Phase 2 — eval server implementation
  )
