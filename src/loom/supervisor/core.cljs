(ns loom.supervisor.core
  "Host-side supervisor. Manages container lifecycle and version history.")

(defn main []
  (println "Loom supervisor starting...")
  ;; TODO: Phase 3 — supervisor implementation
  )
